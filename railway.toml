# https://docs.railway.app/reference/config-as-code
[deploy]
# Lightweight path — no torch/chroma load
healthcheckPath = "/live"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
